import React from 'react';
import TList from "../../components/TodoList/TList";

const TodosPage = () => {
    return (
        <div>
            <TList/>
        </div>
    );
};

export default TodosPage;